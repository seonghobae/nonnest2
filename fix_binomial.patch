--- R/llcont.R
+++ R/llcont.R
@@ -53,13 +53,11 @@
              if(is.matrix(y)) {
                ## Bolt: replaced apply(..., 1, sum) with optimized rowSums() for performance
                n <- rowSums(y)
-               y_mat <- y
-               y_res <- n * 0
-               n_cond <- n == 0
-               if (any(n_cond)) y_res[n_cond] <- 0
-               if (any(!n_cond)) y_res[!n_cond] <- y_mat[, 1][!n_cond] / n[!n_cond]
-               y <- y_res
+               y <- ifelse(n == 0, 0, y[, 1]/n)
              } else {
                n <- rep.int(1, length(y))
              }
